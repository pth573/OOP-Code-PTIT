package org.example;

import java.io.File;
import java.io.FileNotFoundException;
import java.math.BigInteger;
import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

// Press Shift twice to open the Search Everywhere dialog and type `show whitespaces`,
// then press Enter. You can now see whitespace characters in your code.
public class Main {
    public static void main(String[] args) throws FileNotFoundException,NumberFormatException  {
        try
        {
//
            Scanner sc = new Scanner(new File("G:\\OneDrive - ptit.edu.vn\\Desktop\\JAVA ptit\\th16\\src\\main\\java\\org\\example\\DATA.in"));
//            BigInteger sum = new BigInteger("0");
//            List<String> list = new ArrayList<String>();
//            while(sc.hasNext())
//            {
//                String s = sc.next();
//                String str = "";
//                for (int i = 0; i < s.length(); i++) {
//                    if (!('0' <= s.charAt(i) && s.charAt(i) <= '9'))
//                        str += ' ';
//                    else str += s.charAt(i);
//                }
//                String[] arr = s.split("\\s+");
//                for (String k : arr) {
//                    list.add(k);
//                }
//            }
//            long x = (long)Integer.MIN_VALUE;
//            long y = (long)Integer.MAX_VALUE;
//            long a = Long.MIN_VALUE;
//            long b = Long.MAX_VALUE;
//
//            for(int i = 0; i < list.size(); i++)
//            {
//                String s = list.get(i);
//                long tmp = Long.parseLong(s);
//                System.out.println(tmp);
//                if((a <= tmp && tmp < x) || (y < tmp && tmp <= b))
//                {
//                    sum.add(new BigInteger(s));
//                }
//            }
//           System.out.println(sum.toString());
        }
        catch (NumberFormatException e)
        {

        }
    }
}